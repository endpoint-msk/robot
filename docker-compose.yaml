version: "3.9"
services:
  bot:
    hostname: endpoint-webapp
    build:
      context: .
    restart: always
    networks:
      - public
    env_file:
      - .env
    volumes:
      - ./bot-data:/app/bot-data
networks:
  public:
    name: public
    external: true
